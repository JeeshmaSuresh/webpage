initiaization - git init
use details - git config --global user.name ""
git config --global user.email ""
git config --list - press q to stop
git status
git add -A
git commit -m "message"
git remote add name path - we can give any name we like but no repeted names
git push origin(name)  master

